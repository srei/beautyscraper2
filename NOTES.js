CLIENT:
	* react
		* react router
		* redux (state)
		* react-thunk (async)

SERVER:
	* node
		* express
		* ORM --> mongoose

DATABASE:
	* MongoDB
		* robomongo --> to see your database
	* SQL
		*GUI

TESTING:
	* mocha -- provides test structure
	* chai -- provides assertion library
	* chai-enzyme -- another assertion libarry SPECIFIC for react.
			- // we can use 3 assertions (expect, assert || should w/ chai enzyme && chai)
	* enzyme -- to unit test the front end
	* SINON (for async)
	* Supertest (backend)
	* Selenium (Front end)


BUILD TOOLS:
* Webpck:
* Web-dev- server


1) get babel to work w/ mocha: downl
2) create middleware so taht it doesnt call chai etc etc every time --> mocha is run w/ node -- setup.js is run everytime.


SETTING UP SERVER
--> using expres/ node .. Express is a web framework, node.js web application

- JSX = creates hook

// create REACT COMPONENNT
//
//
//
//robo